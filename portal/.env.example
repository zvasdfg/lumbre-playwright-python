# Optional explicit environment override.
# Defaults: `npm run dev` uses development and a production build uses production.
# Keep both values aligned because server code reads LUMBRE_ENV and client code
# receives NEXT_PUBLIC_LUMBRE_ENV at build time.
LUMBRE_ENV=development
NEXT_PUBLIC_LUMBRE_ENV=development
