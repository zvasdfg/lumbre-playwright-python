BASE_URL=http://127.0.0.1:3000
HEADLESS=true
DEFAULT_TIMEOUT_MS=10000
AUTOMATION_PROJECT=Lumbre
LOCALE=es-MX
VIEWPORT_WIDTH=1440
VIEWPORT_HEIGHT=1000
# Populated by the managed parallel runner; do not point multiple workers at one mutable SUT.
AUTOMATION_WORKER_BASE_URLS=
PLAYWRIGHT_PROXY=
